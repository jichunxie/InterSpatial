% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_wasserstein_distance.R
\name{compute_wasserstein_distance}
\alias{compute_wasserstein_distance}
\title{Compute Pairwise Wasserstein Distances Between Cluster Probabilities}
\usage{
compute_wasserstein_distance(
  prob_data,
  source_cell_id,
  receiver_cell_id,
  source_name = NULL,
  receiver_name = NULL,
  Distance_Matrix,
  out_file_prefix = "wass_distance_meta_cells",
  chunk_size = 10000,
  out_dir = ".",
  nCores = NULL,
  parallelize = TRUE
)
}
\arguments{
\item{prob_data}{A matrix (meta-cells × spatial spots) of normalized probabilities.}

\item{source_cell_id}{Integer vector of row indices in \code{prob_data} indicating source meta-cells.}

\item{receiver_cell_id}{Integer vector of row indices in \code{prob_data} indicating receiver meta-cells.}

\item{Distance_Matrix}{A numeric distance matrix (spots × spots), typically from \code{create_distance_matrix()}.}

\item{out_file_prefix}{Prefix for naming output RDS files.}

\item{chunk_size}{Integer. Number of meta-cell pairs to compute per batch (default: 10,000).}

\item{out_dir}{Output directory for saving distance files (default: current directory).}

\item{nCores}{Integer. Number of CPU cores to use (default: all available minus 2, up to max 20).}

\item{parallelize}{Logical. Whether to use parallel computing (default: TRUE).}
}
\value{
A data frame with Wasserstein distances between meta-cell pairs.
}
\description{
Computes Wasserstein distances between all source and receiver meta-cell pairs
based on their probability distributions across spatial spots.
}
